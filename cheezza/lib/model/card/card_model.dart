


class card_model {
   String image;
   String name;
  String desc;
  double rank;
  double price;
  String size;
  int count;

   card_model(this.image, this.name, this.desc, this.rank, this.price, this.size,
      this.count);
}